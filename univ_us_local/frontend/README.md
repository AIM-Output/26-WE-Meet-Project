# frontend

`univ_us_local` 의 Next.js 프론트. 실행·구조는 상위 폴더의 [README.md](../README.md) 를 볼 것.

- `npm run dev`   개발 (3000, /api 는 FastAPI 8000 으로 rewrite)
- `npm run build` 정적 export → `out/` (FastAPI 가 서빙)
