# WE-Meet(유니버스) 서비스 제공 플랫폼·서버 구성 검토

- **작성일**: 2026-09-21 (검토 기간 2026-09-18 ~ 09-21)
- **근거**: `eclass_agent/`·`notice_agent/` 코드와 실행 로그, `WE-Meet_프로젝트계획서(수정).docx`
- **목적**: 아래 세 요구를 만족하는 제공 방식(웹앱·크롬 익스텐션·모바일 앱·로컬 에이전트·메신저)을 비교하고, **서버가 꼭 필요한 범위**를 확정한다.

---

## 1. 요구사항

1. **무인 수집**: 과제·강의자료는 교수가 언제 올릴지 모른다. 내 PC가 오래 꺼져 있어도 정해진 시각에 크롤링이 돌아야 한다. (주기는 별도로 결정)
2. **모바일**: 주 서비스는 노트북/데스크탑. 아침 브리핑 같은 일부 알림은 Android/iOS에서 받을 수 있어야 한다.
3. **개인정보**: e클래스 에이전트의 계정 정보 저장 방식을 기준으로 보호 방안을 세운다. 특히 **로그인 정보(SSO 비밀번호·세션)를 서버에 두지 않는 방향**을 우선 검토한다.

---

## 2. 코드·로그에서 확인한 전제 사실

| 확인 사항 | 근거 | 설계에 미치는 영향 |
|---|---|---|
| e클래스/SSO 세션은 **2시간 남짓이면 만료** | `eclass_agent/state/sync.log`: 09-16 10:32 → 12:40 실행 사이에 이미 "세션 만료" | 세션 쿠키만으로는 무인 유지 불가 |
| **SSO 쿠키만으로 복구(`refresh_via_sso`) 성공 사례 0건** — 매번 비밀번호 로그인으로 넘어감 | 로그 전체에 "SSO 쿠키로 재접속 성공" 없음 | 현재 구조를 그대로 서버로 옮기면 **서버에 비밀번호(암호화) 보관이 사실상 필수** |
| 신뢰기기 = "PC"가 아니라 **쿠키**(`RathonSSO_TrustDevice_*`, 약 1년) | `eclass_agent/AUTOMATION.md`, `login.py` | 쿠키를 옮기면 그곳이 신뢰기기가 됨. 2차 인증만 면제하며 **그것만으로 로그인은 안 됨**. IP가 바뀌어도 유지되는지는 실측 필요 |
| `state/cred.bin`은 DPAPI 암호화, **`state/storage_state.json`은 평문** | `eclass_agent/auth.py`, `config.py` | 쿠키 25개 안에 신뢰기기 쿠키·세션 토큰·`secEmployeeName/No`·`secDepartment` 등 **이름·학번·학과가 평문** |
| Task Scheduler는 "로그인 중에만" 실행 | `eclass_agent/register-task.ps1` | 절전은 `WakeToRun`으로 깨울 수 있으나 **전원 OFF는 불가** → 요구 1은 원격 실행이 정답 |
| **e클래스가 Moodle 모바일 웹서비스를 켜 둠** (`enablewebservices=1`, `enablemobilewebservice=1`, `typeoflogin=1`) | 공개 엔드포인트 `POST https://sel.jnu.ac.kr/lib/ajax/service-nologin.php` (`tool_mobile_get_public_config`, 로그인 불필요)로 확인. `login/token.php`도 `invalidlogin` 응답(서비스 존재) | 브라우저 없이 **REST 토큰**으로 과제·캘린더·파일 수집 가능성 → 비밀번호 대신 토큰만 보관하는 길 |
| F11~F13 수집 소스 6개 중 **5개는 로그인 불필요** | `notice_agent/scripts/config.py: SOURCES` | SSO가 필요한 것은 학사정보시스템 카탈로그(`hakstd_catalog`)와 프로필 자동 채움(`profile_from_hakstd`)뿐 |

---

## 3. 서버가 "나 대신" 들어가려면 — 증표 3종

> e클래스는 우리 서버를 모른다. 서버가 새벽에 나 대신 들어가려면 e클래스가 믿어 주는 **내 증표를 하나는** 들고 있어야 한다. 증표는 비밀번호·쿠키·토큰 세 가지이고, 무엇을 맡기느냐에 따라 편의성과 위험이 달라진다.

| | 비밀번호 | 쿠키 (세션 + 신뢰기기) | 토큰 (Moodle 웹서비스 토큰) |
|---|---|---|---|
| **비유** | **마스터키** — SSO로 묶인 집 전체(e클래스·학사정보시스템·포털·메일)를 엶 | **출입증** — 로그인하면 e클래스가 발급, 몇 시간 뒤 만료. 신뢰기기 쿠키는 "얼굴 아는 사이" 스티커라 2차 인증만 건너뜀 | **방 하나짜리 열쇠** — e클래스가 앱용으로 발급, e클래스 안에서만 통함 |
| **수집 때 서버가 하는 일** | 매번 브라우저를 띄워 SSO 폼에 ID/PW 입력 | 갖고 있는 출입증을 내밂 | 토큰을 붙여 API 호출 (브라우저 불필요) |
| **만료되면** | 만료 없음(비밀번호 변경 전까지) | 약 2시간 뒤 죽고, 새 출입증은 결국 비밀번호가 필요 → **쿠키만으론 무인 불가** | 수 주~무기한(사이트 설정). 만료 시 사용자가 한 번 재로그인 |
| **서버가 털리면** | 공격자가 **SSO 전체** 로그인 가능(성적·등록금·개인정보) | 살아 있는 몇 시간 동안 열람 가능. 신뢰기기 스티커는 1년간 2차 인증 면제 이점을 줌 | **e클래스 열람만**, 로그인 자체는 불가 |
| **내가 끄는 법** | 학교 비밀번호 변경 | SSO 로그아웃/신뢰기기 해제 | e클래스 '보안 키' 페이지에서 토큰 삭제(즉시 무효) |
| **현재 코드** | `state/cred.bin` (DPAPI) — 완전 무인 모드 | `state/storage_state.json` (평문) | 없음 — 실험으로 발급 가능 여부 확인 필요 |

**권장 순서: 토큰 > 쿠키(보조) > 비밀번호(사용자가 명시적으로 동의한 경우만).** 토큰이 안 되는 항목(예: 유비온 게시판 `ubboard`)이 있으면 그 부분만 쿠키+비밀번호 모드로 하거나, PC가 켜져 있을 때 로컬이 채운다.

---

## 4. 요구 1 — 크롤러를 어디서 돌릴 것인가

| 방식 | 장점 | 단점 | 판단 |
|---|---|---|---|
| **A. 클라우드 VM 워커** (계획서의 Docker Compose에 `crawler` 컨테이너 추가, `mcr.microsoft.com/playwright/python` 이미지) | 계획서 아키텍처와 일치·상시 가동·사용자별 스케줄을 DB에 두고 APScheduler로 실행·재시도/로그 통제 쉬움 | 서버가 인증정보 보관(6절)·비용(서울 리전 소형 VM 월 0~1만 원대, Oracle Always Free 춘천/서울이면 0원)·Chromium 메모리 1GB+ | 서버 수집형의 1순위 |
| B. 서버리스 잡 (Cloud Run Jobs + Cloud Scheduler, Lambda) | 쓴 만큼 과금·서버 관리 없음 | 콜드스타트·Playwright 패키징·세션 파일 외부 저장·실행시간 제한 | VM이 있으면 불필요 |
| C. GitHub Actions cron | 무료·설정 5분 | 러너가 해외 IP라 학교가 막을 수 있음·cron 지연 분~시간·비밀번호가 GitHub Secrets에 | 개인용 임시책까지만 |
| D. 집 상시기기 (라즈베리파이·구형 노트북) | 인증정보가 내 하드웨어를 안 떠남·전기세 외 무료 | 타 사용자에게 제공 불가·장애 시 직접 복구 | "완전 로컬" 개인 옵션 |
| E. 현재 방식 (PC Task Scheduler) | 이미 동작·DPAPI | 요구 1 불충족 | 폴백/개발용 유지, **로컬 수집형(8절)의 기본** |

A를 택할 때 신경 쓸 것: **서울 리전** 필수(해외 IP 차단 가능성), 사용자별 스케줄에 ±몇 분 지터, 전역 동시성 1~2. 토큰 API가 되면 "캘린더/알림 목록만 자주 확인 → 변화 있을 때만 전체 수집"이 가능해 주기를 짧게 잡아도 학교 서버 부담이 작다.

---

## 5. 요구 2 — 모바일(Android/iOS) 알림

| 방식 | 장점 | 단점 | 판단 |
|---|---|---|---|
| **A. PWA + Web Push** (Next.js에 manifest·service worker, FastAPI에 `pywebpush`/VAPID) | 코드 하나로 데스크톱·모바일·앱스토어 없음·계획서 "1차 웹 푸시"와 일치·Android 완전 지원 | **iOS는 16.4+에서 '홈 화면에 추가' 후에만** 푸시 수신·백그라운드 작업 없음 | 1순위 |
| **B. 메신저 봇** (Telegram/Discord 봇, 이메일, 카카오 '나에게 보내기') | 설치 0·iOS도 확실히 도착·구현 반나절·`/today` 같은 명령 가능 | 브랜딩 없음·카카오는 팀원 외 일반 사용자에게 열려면 비즈앱(사업자) 필요 | A의 폴백으로 병행. 아침 브리핑은 여기로 보내도 충분 |
| C. 네이티브 앱 (Expo/React Native, Flutter) | FCM/APNs로 가장 안정·위젯 | Apple $99/년·Play $25·심사·코드베이스 추가·폰에서 크롤링은 어차피 불가 | 후속 과제 |
| D. 크롬 익스텐션 | — | 모바일 브라우저는 익스텐션 미지원 | 해당 없음 |
| E. Google Calendar 리마인더 | 우리 서버 없이 Google이 폰에 배달 | 형식이 캘린더 알림에 한정 | 로컬 수집형에서 F10·마감 알림 대체 채널 |

---

## 6. 요구 3 — 계정 정보·개인정보

### 6.1 현재 로컬 도구 진단

**강점**: 비밀번호는 DPAPI(CurrentUser + 앱 엔트로피)로만 존재, 평문 미기록, `setup-creds.cmd --clear`로 즉시 삭제, 반자동(비밀번호 미저장) 모드 선택 가능, `state/`·`data/` gitignore, `notice_agent`의 `profile.json`에 이름·학번 미저장, LLM 프롬프트에 이름·학번 미전달.

**약점과 즉시 조치**
1. `storage_state.json` 평문 → `auth.py`의 `_protect/_unprotect`를 그대로 써서 암호화. Playwright `new_context(storage_state=…)`는 **dict도 받으므로** 복호화 내용을 파일로 쓰지 않고 메모리에서 바로 넘길 수 있다.
2. DPAPI는 "파일 도난·다른 사용자"에 대한 보호이지 **같은 계정으로 실행되는 악성코드는 막지 못한다**(엔트로피가 소스에 있음). 크롬 비밀번호 저장소와 같은 수준임을 문서에 명시.
3. 로그인 실패 시 `state/login_debug.png`에 아이디가 찍힐 수 있음 → 마스킹 또는 보관기간 제한. `state/`가 OneDrive 등 동기화 폴더 밖인지 확인(현재는 밖).
4. `eclass_agent/auth.py:14`의 `ENTROPY = b"eclass-agent/jnu/v1"`는 폴더명이 아니라 암호화 키 재료 → **절대 변경 금지**(기존 `cred.bin` 복호화 불가).

### 6.2 서버로 옮길 때의 보관 모드 (사용자가 선택)

| 모드 | 서버에 남는 것 | 무인 수집 | 유출 시 피해 범위 |
|---|---|---|---|
| ① 로컬 전용 (현재) | 없음 | PC 켜져 있을 때만 | 없음 |
| ② 토큰/세션 위탁 | Moodle 웹서비스 토큰(되면) 또는 세션+신뢰기기 쿠키 | 토큰이면 가능, 쿠키만이면 2시간 한계 | e클래스 한정·사용자가 즉시 무효화 가능 |
| ③ 완전 위탁 (옵트인) | 암호화된 SSO 비밀번호 | 가능 | **SSO 전체**(학사정보시스템·포털·메일) |

③을 제공한다면: 사용자별 데이터키(AES-256-GCM) + 마스터키는 워커 컨테이너 환경/KMS에만(DB·이미지·git에 없음) → DB 덤프만으론 복호화 불가. **API 컨테이너는 vault 테이블 읽기 권한 없음**(워커만). 복호화마다 감사 로그, "연결 해제" 즉시 행 삭제, 동의 문구에 "학사정보시스템 등 SSO 전체 접근 가능" 명시. Windows DPAPI는 리눅스에 없으므로 `auth.py`를 `SecretStore` 인터페이스(로컬=DPAPI/keyring, 서버=봉투암호화)로 추상화.

### 6.3 서버 온보딩(2차 인증)도 가능

사용자가 웹앱에 ID/PW 1회 입력 → 서버 headless 브라우저가 SSO 폼 제출 → `#btnMfaFidoPush`로 학교 앱 푸시 → 사용자가 폰에서 승인 → 서버 컨텍스트가 신뢰기기가 되고, 선택한 모드에 따라 비밀번호는 즉시 폐기. 현재 `login.py` 흐름을 headless로 옮긴 것과 같다.

### 6.4 그 외

- `profile.json`의 장애·소득분위·보훈 여부는 개인정보보호법상 **민감정보**(별도 동의·암호화). 학번 대신 가명 ID.
- 학교 공지 "계정정보 공유 금지"는 팀원 본인 계정을 팀 서버에 두는 단계까진 해석 여지가 있지만, 계획서의 **교내 시범 서비스** 확대 전에는 정보전산원 협의 필요(계획서 "LMS 공식 연동 협의"와 연결).

---

## 7. 플랫폼 종합 비교

| 플랫폼 | 요구1 무인수집 | 요구2 모바일 | 요구3 개인정보 | 개발/배포 비용 | 우리 스택 적합성 |
|---|---|---|---|---|---|
| **웹앱(Next.js PWA) + 서버** | ◎ 서버 워커가 수행 | ○ Web Push(iOS는 홈화면 추가 조건) | △ 서버가 토큰/쿠키/비밀번호 중 하나 보관 | 낮음(계획서 그대로), Vercel 무료 + VM | ◎ |
| **크롬 익스텐션** | ✕ 브라우저 켜져 있을 때만(`chrome.alarms`) | ✕ | **◎ 저장할 비밀번호가 없음** — 사용자 브라우저가 이미 로그인·신뢰기기 | 중간(MV3 별도 코드, 웹스토어 $5·심사), Chrome/Edge만 | ○ 보조용: e클래스 화면에 "캘린더 등록" 버튼 주입, 세션을 서버에 넘기는 '세션 릴레이' |
| 네이티브 앱 | ✕ 폰에서 크롤링 불가 | ◎ FCM/APNs | ○ | 높음($99/년 + 심사 + 코드베이스) | △ 후속 |
| 로컬 에이전트(현재, 트레이 앱화 가능) | △ PC 켜져 있을 때만 | ✕ | ◎ DPAPI, 내 PC 밖으로 안 나감 | 0 (이미 있음) | ○ 개인·개발용, 로컬 수집형의 핵심 |
| 메신저 봇/이메일 | (알림 채널) | ◎ 설치 0 | ○ 메시지 본문만 제3자 경유 | 매우 낮음 | ◎ 폴백 채널 |

크롬 익스텐션의 진짜 가치는 "PC가 켜져 있는 동안엔 비밀번호 없이 익스텐션이 수집/세션을 공급하고, 꺼져 있을 땐 서버가 이어받는" 하이브리드인데, 수집기를 JS와 Python 두 벌로 유지해야 하므로 1단계엔 넣지 않는다.

---

## 8. 로컬 수집을 가정할 때 서버가 필요한 기능

전제: e클래스 로그인·크롤링과 SSO가 필요한 모든 것(학사정보시스템 `hakstd`)은 각 사용자 PC(로컬)에서 돈다.

### 8.1 기능별 판정

| 기능 | 필요한 것 | 로컬만으로? | 서버가 필요한 이유 / 서버 없는 대안 |
|---|---|---|---|
| F1 학사일정 자동 등록 | 학교 공지(공개)·Gmail → LLM 추출 → 캘린더 등록 | ○ | Google OAuth는 데스크톱 앱 유형(루프백 리다이렉트)이면 서버 없이 됨. 마감 알림은 Google Calendar 리마인더가 배달 |
| F2 졸업요건 트래커 | 이수내역(hakstd, SSO) + 규칙 계산 | ○ | SSO라서 오히려 로컬이어야 함 |
| F3 출결·학사경고 예방 | 출결 데이터 + 규칙 계산 | ○ | 현재 크롤러는 출결 모듈을 건너뜀(`eclass_agent/config.py`) → 데이터 원천(수기/추가 수집)은 별도 결정 |
| F4 강의자료 RAG | 로컬 PDF → Document Parse/임베딩 API → 벡터 저장 → 질의 | ○ | pgvector 대신 로컬 벡터 DB(Chroma·LanceDB·sqlite-vec). 파일이 PC를 안 떠나 "수집물 외부 미배포" 준수도 쉬움 |
| F5 시험 공부 일정 추천 | 계산 + 캘린더 등록 | ○ | — |
| F6 과제 마감 자동 등록 | e클래스 크롤링 | ○ (가정) | — |
| F7 과제 우선순위 | 계산 | ○ | — |
| F8 공강 학습 플랜 배치 | 시간표 + 마감 → 캘린더 등록 | ○ | — |
| F9 자연어 일정 등록·질의 | LLM Function Calling + 로컬 데이터 | ○ PC에서만 | **폰에서 대화하려면 서버 필요** |
| F10 아침 브리핑 | 매일 정해진 시각에 생성·발송 | **✕** | 07:00에 PC가 꺼져 있으면 아무도 안 보냄. 대안 ① Google Calendar에 "아침 브리핑" 이벤트+알림 등록 ② PC를 절전으로 두고 `WakeToRun` ③ 얇은 서버 |
| F11 장학 매칭·신청서 | 공개 게시판 5곳 + hakstd(SSO) + LLM + 규칙 | ○ | 로컬 가능. PC가 며칠 꺼져 있으면 신청기간 짧은 공지를 놓칠 수 있음 |
| F12 맞춤 기회 알림 | F11과 같은 수집기(공개) | ○ | 위와 동일 |
| F13 사업단 공지 통합 | F11과 같은 수집기(공개) | ○ | 위와 동일 |
| F16 팀플 일정 조율 | **팀원들의** 공강·역할·마감 | **✕** | 남의 데이터가 내 PC에 없음. Google Calendar free/busy 공유는 전원이 캘린더를 서로 공개해야 해 약함 |
| F17 사업단 프로젝트 플랫폼 | 멘토 프로필·과제 신청·보고서 업로드·승인 | **✕** | 본질이 다중 사용자 웹 플랫폼. 로컬 대안 없음 |

### 8.2 기능 밖의 공통 요소

| 요소 | 로컬만으로? | 비고 |
|---|---|---|
| 웹 UI(Next.js) | ○ | `localhost` 또는 Tauri/Electron. **폰에서 보는 대시보드**는 서버 필요 |
| 알림 발송(Web Push·텔레그램·이메일) | △ | 보내는 코드는 로컬에서도 돌지만 "PC 꺼진 시각의 알림"은 불가 → Google Calendar 리마인더에 얹거나 서버 |
| LLM API 키 | ○ (팀원 각자 `.env`) | 시범 사용자에게 배포하면 키가 클라이언트에 심기므로 그때는 서버 프록시 필요 |
| Google OAuth(Calendar·Gmail) | ○ | 데스크톱 앱 클라이언트로 서버 없이 가능. 앱을 '테스트' 상태로 두면 7일마다 재동의 → 프로덕션 게시 필요 |
| DB | ○ | SQLite. 서버가 생기면 그쪽만 Postgres |
| 계획서 마일스톤 "배포 환경에서 상시 시연" | ✕ | 데모용 호스팅은 필요 |
| 사용자 확대 | — | 로컬 수집이면 **모든 사용자가 Python+Playwright 설치**(설치 프로그램/트레이 앱 배포) 필요 |

### 8.3 "얇은 서버" 구조

크롤링을 로컬에 두면 서버의 역할은 **배달·모바일·공유**로 줄고, SSO 비밀번호·세션·강의자료 파일은 서버에 올라가지 않는다.

```
[로컬 에이전트: 각 사용자 PC]                      [얇은 서버: 서울 리전 소형 VM]
 e클래스/hakstd 수집 (SSO, DPAPI)                    ← 업로드: 마감·과제 상태·과목명·매칭 결과·
 문서 추출·RAG·우선순위·학습블록 계산                  (미리 만들어 둔) 브리핑 텍스트  ※ 자격증명·원본 파일 없음
 Google Calendar 등록 (OAuth 루프백)                  07:00 브리핑 배달 (Web Push / 텔레그램 / 이메일)
 브리핑 텍스트 생성 후 업로드                          폰용 대시보드·F9 채팅 조회
                                                     F16 팀 공유 상태 · F17 플랫폼
                                                     공개 게시판(F1·F11~F13) 수집·요건 추출 (9절)
```

- **서버 필수**: F10 정시 배달, 폰에서 보기/대화(F9), F16, F17, 상시 데모.
- **서버 없이 가능**: F1~F8, F11~F13 전부. F10과 마감 알림도 Google Calendar를 배달 채널로 쓰면 서버 없이 80%는 된다.
- **대가**: 브리핑·장학 알림의 신선도가 "마지막으로 PC를 켠 시점"에 묶이고, 사용자마다 로컬 에이전트를 설치해야 한다.

---

## 9. F11~F13 — 로그인 필요 여부와 서버/로컬 분할

| 소스 (`notice_agent/scripts/config.py`) | 로그인 | 비고 |
|---|---|---|
| 전남대 홈페이지 › 장학안내 | 불필요 | 공개 |
| 인공지능학부 공지 (장학 말머리) | 불필요 | 공개 |
| AI융합대학 공지 | 불필요 | 공개 |
| AICOSS 사업단 공지 | 불필요 | 공개 |
| 국제협력과 공지 | 불필요 | 공개 |
| **학사정보시스템 › 전체 장학 안내 (`hakstd_catalog`)** | **SSO** | 교내·법정·교외 지원조건 전문. 카탈로그라 자주 안 바뀜 |

F12(공모전·모집공고 `cate=15/16`, AICOSS 경진대회)·F13(사업단 게시판 추가)은 같은 공개 수집기에 소스만 늘리는 것이라 로그인이 없다. SSO를 쓰는 곳은 `hakstd_catalog`와 **`profile_from_hakstd`**(기이수성적에서 학년·평점·이수학점 읽어 프로필 자동 채움, 가끔 1회)뿐이다.

**분할**: "로그인 필요 = 로컬, 불필요 = 서버"
- **서버**: 공개 게시판 5곳 수집 → 신규 판별 → 자격요건 구조화 추출. 로그인 정보도 개인정보도 쓰지 않음(공지는 공개 데이터). PC가 꺼져 있어도 신선도 유지.
- **로컬**: `hakstd_catalog`(주 1회나 PC 켤 때), `profile_from_hakstd`. 결과는 업로드하거나 로컬에만.

**매칭 단계의 프로필 위치**(로그인 정보와 별개의 개인정보: 학년·전공·이수학점·평점, 민감정보: 소득분위·장애·보훈)

| 방식 | 서버에 남는 개인정보 | 알림 시점 |
|---|---|---|
| ① 매칭도 로컬 | 없음 (서버엔 공지+요건만) | PC를 켰을 때 |
| ② 최소 프로필만 서버 (학년·전공·이수학점·평점) | 기본 학적 정보만 — 1차 매칭 | 서버가 즉시 "해당 가능/확인 필요"로 알리고, 소득분위 등은 로컬에서 2차 확정 |
| ③ 프로필 전체 서버 (암호화+별도 동의) | 민감정보 포함 | 즉시, 완전 매칭 |

"로그인 정보 0"만 목표면 ①②③ 모두 만족, "개인정보 0"까지 원하면 ①. 신청서 초안(F11)의 `draft_context`(진로·활동·가정형편 서술)도 로컬에서 생성하고 서버 DB에는 남기지 않을 수 있다.

---

## 10. 후보 구조 2안

| | **안 A. 서버 수집형** | **안 B. 로컬 수집 + 얇은 서버** (요구 3 우선) |
|---|---|---|
| e클래스·hakstd 수집 | 서울 리전 VM 워커 (토큰 > 쿠키 > 옵트인 비밀번호) | 각 사용자 PC (DPAPI, 현재 `eclass_agent`) |
| 공개 게시판(F1·F11~F13) | 서버 | 서버 |
| 서버에 있는 인증정보 | 토큰 또는 암호화 비밀번호 | **없음** |
| 신선도 | 항상 최신 | 마지막으로 PC 켠 시점 |
| 모바일 | PWA + Web Push + 메신저 폴백 | 동일 (데이터는 로컬이 업로드) |
| 사용자 확대 | 웹 가입만으로 가능 | 로컬 에이전트 설치 필요 |
| 개인정보·약관 부담 | 큼 (SSO 증표 위탁, 정보전산원 협의) | 작음 (프로필 위치만 결정) |
| 공통 | Next.js PWA + FastAPI + PostgreSQL, Web Push/메신저, Google Calendar OAuth, APScheduler(브리핑 배달) | |

---

## 11. 먼저 할 실험 (각 반나절 이내)

1. **Moodle 토큰 실험** — 로그인된 Playwright 세션으로 `/admin/tool/mobile/launch.php?service=moodle_mobile_app&passport=…&urlscheme=moodlemobile`을 열어 토큰이 나오는지, 그 토큰으로 `mod_assign_get_assignments`·`core_calendar_get_calendar_upcoming_view`·`core_course_get_contents`가 되는지, 유비온 게시판(`ubboard`) 함수가 있는지, 토큰 만료가 얼마인지. 되면 안 A에서 비밀번호·브라우저가 필요 없어지고, 안 B의 로컬 에이전트도 Playwright 없이 가벼워진다.
2. **신뢰기기 이식 실험** (안 A용) — `storage_state.json`을 다른 네트워크(핫스팟/VM)로 옮겨 `login.py --auto`가 2차 인증 없이 통과하는지, 학교 서버가 클라우드 IP를 막지 않는지.
3. **iOS PWA 푸시 실험** — 팀원 아이폰에서 홈 화면 추가 후 Web Push 수신 확인.

---

## 12. 미결·결정 필요 사항

- 크롤링 주기 (사용자 결정 예정)
- 안 A / 안 B 확정 — 현재는 요구 3 때문에 **안 B 방향으로 검토 중**
- 매칭용 프로필 위치 (9절 ①②③)
- F10 배달 채널: Google Calendar 리마인더만으로 갈지, 얇은 서버 푸시까지 둘지
- F3 출결 데이터 원천 (크롤러는 출결 미수집)
- 로컬 에이전트 배포 형태 (설치 프로그램/트레이 앱) — 안 B에서 시범 사용자 확대 시 필수
- 정보전산원 협의 시점 (안 A 또는 교내 시범 서비스 전)
- 즉시 조치: `storage_state.json` 암호화, `login_debug.png` 마스킹

---

## 13. 구현 현황 (2026-09-21)

- **결정**: 안 B 를 "개인 로컬 서버 웹"으로 구현한다 — FastAPI(127.0.0.1:8000) 하나가 API 와 정적 프론트(Next.js export)를 함께 서빙. `MODE=local/server` 분기와 폰 접근(Tailscale vs 얇은 서버)은 아직 미결.
- **만든 것**: `univ_us_local/` — 메인 화면 1차. 왼쪽/가운데 캘린더(월·주·목록, e클래스 마감 + 내 일정 등록·수정·드래그), 오른쪽 패널(오늘·마감 임박·브리핑 자리·기능 타일). 실행·API 는 `univ_us_local/README.md`.
- **다음**: 오른쪽 패널 기능을 F7 → F9 → F10 순으로 채운다. 브리핑 스케줄러는 백엔드 프로세스에 내장(APScheduler)할 예정.
